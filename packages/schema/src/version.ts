/**
 * Bumped whenever `ProjectDoc` changes shape. Every document records the version it
 * was written with, and a migration runs on load (PLAN.md §3, risk register §13).
 */
export const SCHEMA_VERSION = 1;
