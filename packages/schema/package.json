{
  "name": "@ui-builder/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The contract shared by the studio, the API and the code generator. Depends on nothing else in the repo.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}
