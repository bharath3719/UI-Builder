{
  "name": "@ui-builder/codegen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ProjectDoc -> a runnable React project, as files in memory. Pure; shared by the studio's code panel and the API's export route.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ui-builder/components": "*",
    "@ui-builder/schema": "*"
  }
}
